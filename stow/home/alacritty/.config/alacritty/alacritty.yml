# Configuration for Alacritty, the GPU enhanced terminal emulator.

# Import additional configuration files
#
# Imports are loaded in order, skipping all missing files, with the importing
# file being loaded last. If a field is already present in a previous import, it
# will be replaced.
#
# All imports must either be absolute paths starting with `/`, or paths relative
# to the user's home directory starting with `~/`.

import:
    - "~/.config/alacritty/environment.yaml"
    - "~/.config/alacritty/window.yaml"
    - "~/.config/alacritty/scrolling.yaml"
    - "~/.config/alacritty/font.yaml"
    - "~/.config/alacritty/colors.yaml"
    - "~/.config/alacritty/bell.yaml"
    - "~/.config/alacritty/selection.yaml"
    - "~/.config/alacritty/cursor.yaml"
    - "~/.config/alacritty/hints.yaml"
    - "~/.config/alacritty/keybindings.yaml"
    - "~/.config/alacritty/debug.yaml"

# Live config reload (changes require restart)
live_config_reload: true
